const mongoose = require('mongoose');
// This script is deprecated in favor of the API endpoint /api/colleges/seed-nirf
// But kept for reference if manual CLI seeding is needed.
// It requires the server models to be accessible.

console.log("Use the API endpoint: http://localhost:5001/api/colleges/seed-nirf");
console.log("Or run: curl http://localhost:5001/api/colleges/seed-nirf");
